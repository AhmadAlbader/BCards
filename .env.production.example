# ================================
# PRODUCTION ENVIRONMENT (Railway)
# ================================
# Copy this file and configure in Railway Dashboard
# Railway will auto-inject some variables

# IMPORTANT: Railway provides these automatically:
# - DATABASE_URL (from PostgreSQL plugin)
# - RAILWAY_PUBLIC_DOMAIN
# - RAILWAY_ENVIRONMENT

# Network Configuration (Railway auto-configures)
ENVIRONMENT=production
API_HOST=${{RAILWAY_PUBLIC_DOMAIN}}
API_PORT=443
FRONTEND_HOST=${{RAILWAY_PUBLIC_DOMAIN}}
FRONTEND_PORT=443

# CORS and Security (Custom Domain)
CORS_ORIGINS=https://digitalbc.sword-academy.net
ALLOWED_HOSTS=digitalbc.sword-academy.net,api.digitalbc.sword-academy.net

# Frontend URLs (Custom Domain)
NEXT_PUBLIC_API_HOST=api.digitalbc.sword-academy.net
NEXT_PUBLIC_API_PORT=443
NEXT_PUBLIC_API_URL=https://api.digitalbc.sword-academy.net

# Frontend URL (for Stripe redirects)
FRONTEND_URL=https://digitalbc.sword-academy.net

# Database (Railway provides DATABASE_URL automatically)
# DATABASE_URL=postgresql+asyncpg://user:pass@host:port/db (auto-injected)
DB_USER=postgres
DB_PASSWORD=${{POSTGRES_PASSWORD}}
DB_NAME=railway

# Security (CRITICAL: Generate new strong key for production!)
# Generate using: openssl rand -base64 64
SECRET_KEY=REPLACE_WITH_STRONG_PRODUCTION_KEY_MINIMUM_64_CHARACTERS_RANDOM

# Stripe (Production Keys - Get from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_live_YOUR_LIVE_SECRET_KEY_HERE
STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_LIVE_PUBLISHABLE_KEY_HERE
STRIPE_WEBHOOK_SECRET=whsec_YOUR_PRODUCTION_WEBHOOK_SECRET_HERE

# Subscription Configuration
DEFAULT_TRIAL_DAYS=3
FREE_PLAN_EMPLOYEE_LIMIT=2
PRO_PLAN_EMPLOYEE_LIMIT=50
ENTERPRISE_PLAN_EMPLOYEE_LIMIT=999999

# Pricing (USD)
PRO_PLAN_PRICE_USD=29.00
ENTERPRISE_PLAN_PRICE_USD=99.00

# Pricing (KWD)
PRO_PLAN_PRICE_KWD=8.90
ENTERPRISE_PLAN_PRICE_KWD=30.50

# Currency Support
DEFAULT_CURRENCY=USD
SUPPORTED_CURRENCIES=USD,KWD

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=noreply@yourdomain.com

# Debug Mode (MUST be false in production!)
DEBUG=false

# ================================
# RAILWAY DEPLOYMENT NOTES:
# ================================
# 1. Create PostgreSQL database in Railway
# 2. Add these variables in Railway Dashboard (Settings → Variables)
# 3. Railway will auto-inject DATABASE_URL
# 4. Update CORS_ORIGINS with actual domain
# 8. Configure webhook URL in Stripe: https://api.digitalbc.sword-academy.net/api/webhooks/stripe
# 9. Add custom domains in Railway:
#    - Frontend: digitalbc.sword-academy.net
#    - Backend: api.digitalbc.sword-academy.net
# 10. Configure DNS CNAME records in Hostinger (see HOSTINGER_DNS_GUIDE.md)
# 6. Get Stripe production keys from dashboard
# 7. Set DEBUG=false
# 8. Configure webhook URL in Stripe: https://your-domain.railway.app/api/webhooks/stripe
